# Internal docs (not committed — see docs-internal/README if present)
docs-internal/

# Cursor agent rules (local only — keep under ~/.cursor/rules or project .cursor untracked)
.cursor/

.DS_Store
node_modules
/server/node_modules
/server/dist
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
