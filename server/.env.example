PORT=8787
# Mock response for GET /api/v1/subscription (active | trialing | past_due | none)
MOCK_SUBSCRIPTION_STATUS=none
MOCK_TIER=

# Stripe (optional — webhook verifies signature only when both are set)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
